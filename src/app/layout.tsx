import type { Metadata } from "next";
import { Poppins, Rubik } from "next/font/google";
import localFont from "next/font/local";
import "./globals.css";

const poppins = Poppins({
  weight: "400",
  subsets: ["latin", "hebrew"], 
  variable: "--font-poppins",
});

const rubik = Rubik({
  weight: "400",
  subsets: ["latin", "hebrew"],
  variable: "--font-rubik",
});

const geistSans = localFont({
  src: "./fonts/GeistVF.woff",
  variable: "--font-geist-sans",
  weight: "100 900",
});
const geistMono = localFont({
  src: "./fonts/GeistMonoVF.woff",
  variable: "--font-geist-mono",
  weight: "100 900",
});
/*
const rubikDoodleShadow = localFont({
  src: "/fonts/rubikDoodleShadow-Regular.ttf", 
  variable: "--font-rubik-doodle-shadow",
});
const rubikDirt = localFont({
  src: "/fonts/RubikDirt-Regular.ttf",
  variable: "--font-rubik-dirt",
});
*/
export const metadata: Metadata = {
  title: "Create Next App",
  description: "Generated by create next app",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="he" dir="rtl">
      <body
        className={`${poppins.variable} ${rubik.variable} ${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
